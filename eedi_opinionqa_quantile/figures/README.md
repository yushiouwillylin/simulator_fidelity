# EEDI and OpinionQA Figure Archive

This directory stores archived PNG copies of the final EEDI and OpinionQA paper figures generated by `EEDI_OpinionQA_quantile_construction.ipynb`.

Only the final paper figures are kept here so the reproduction path stays unambiguous.

| Dataset | Archived PNG | Notebook Block | Notes |
| --- | --- | --- | --- |
| EEDI | `eedi_quantile_fidelity_profiles.png` | `Final EEDI Plot` | Paper Figure 13 reproduction: `\widehat V_\ell(\alpha)` with `k = 50`. |
| OpinionQA | `opinionqa_quantile_fidelity_profiles.png` | `Final OpinionQA Plot` | Paper Figure 14 reproduction: `\widehat V_\ell(\alpha)` under total variation with `k = 100`. |
